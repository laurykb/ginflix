apiVersion: v1
kind: Secret
metadata:
  name: ginflix-app-secrets
  namespace: ginflix
type: Opaque
stringData:
  # Fill these values from your secure source (Vault/ExternalSecrets/CI variables).
  KEYCLOAK_CLIENT_ID: "REPLACE_ME"
  KEYCLOAK_REALM: "REPLACE_ME"
  KEYCLOAK_HOST: "REPLACE_ME"
  MONGO_URI: "REPLACE_ME"
