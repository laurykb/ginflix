apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ginflix
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80        # ce que verront ingress / reverse proxy
      targetPort: 80  # port du conteneur nginx
