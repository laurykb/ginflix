apiVersion: v1
kind: Service
metadata:
  name: streamer
  namespace: ginflix
  labels:
    app: streamer
spec:
  type: ClusterIP
  selector:
    app: streamer
  ports:
    - port: 80
      targetPort: 8080
